const Spinner = () => {
    return (
        <span className='absolute left-[50%] top-[50%] w-[50px] animate-loading h-[50px] border-[5px] border-t-transparent border-solid border-primary-orange rounded-full flex items-center justify-center'></span>
    );
};

export default Spinner;